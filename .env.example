# ==========================================================
# CLEANRR CONFIGURATION - Example Environment Variables
# ==========================================================
# Copy this file to `.env` and fill in the required values.
# Required variables must be set; app will fail fast otherwise.
# ==========================================================

# -------------------------------
# REQUIRED: Core Integrations
# -------------------------------

# Overseerr (base URL without trailing slash + API key)
OVERSEERR_API_URL=http://localhost:5055
OVERSEERR_API_KEY=YOUR_OVERSEERR_API_KEY

# Radarr (base URL without trailing slash + API key)
RADARR_API_URL=http://localhost:7878
RADARR_API_KEY=YOUR_RADARR_API_KEY

# Sonarr (base URL without trailing slash + API key)
SONARR_API_URL=http://localhost:8989
SONARR_API_KEY=YOUR_SONARR_API_KEY

# Redis connection (BullMQ job queue backend)
# For local dev: redis://localhost:6379
# For Docker Compose: redis://redis:6379
REDIS_URL=redis://localhost:6379

# Port for the Express webhook server
OVERSEERR_WEBHOOK_PORT=3000

# -------------------------------
# OPTIONAL: Security / Auth
# -------------------------------
# Require a shared secret for Overseerr webhook requests.
# If set, Overseerr must be configured to send header:
#   cleanrr-secret: <value>
# Leave unset to disable.
# OVERSEERR_WEBHOOK_SECRET=CHANGE_ME_STRONG_SECRET

# -------------------------------
# OPTIONAL: WhatsApp / Baileys
# -------------------------------
# Directory path for Baileys multi-file auth state (persisted WhatsApp session).
# For Docker Compose, this should match the volume mount path.
# Default: ./session (local dev) or /app/session (Docker)
# WHATSAPP_SESSION_PATH=./session

# -------------------------------
# OPTIONAL: Reminder Delays
# -------------------------------
# Time to wait before first reminder for movies (days)
# MOVIE_DELAY_MS=3
# Time to wait before first reminder for TV series (days)
# SHOW_DELAY_DAYS=5
# Time to wait before retry if user doesn’t reply (hours)
# RETRY_INTERVAL_HOURS=24

# -------------------------------
# OPTIONAL: Logging
# -------------------------------
# Log level for Pino: fatal | error | warn | info | debug | trace
# LOG_LEVEL=info
# NODE_ENV affects logging format: 
#   production = structured JSON logs
#   development = pretty-printed logs
# NODE_ENV=development

# OPTIONAL: SQLite location (absolute or relative to project root)
# DB_PATH=./data/cleanrr.sqlite

# -------------------------------
# NOTES
# -------------------------------
# - Wrap values containing special characters in quotes.
# - For Docker deployments, ensure ports are mapped in docker-compose.yml.
# - Redis host changes from "localhost" to "redis" inside Docker network.